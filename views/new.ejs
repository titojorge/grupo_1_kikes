<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Kike</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css"  rel="stylesheet" />
    <link rel="stylesheet" href="/css/new.css">
</head>
<body>
    <%- include('./partials/header')%>
        <h1><%= title %></h1>
        <form action="<%= action %>" method="POST" enctype="multipart/form-data">
            <label for="name">Nombre del producto:</label>
            <input type="text" name="name" value="<%= product ? product.name : '' %>"><br>
            <label for="description">Descripción:</label>
            <textarea name="description"><%= product ? product.description : '' %></textarea><br>
            <label for="image">Imagen:</label>
            <input type="file" name="image"><br>
            <label for="category">Categoría:</label>
            <input type="text" name="category" value="<%= product ? product.category : '' %>"><br>
            <label for="colors">Colores:</label>
            <input type="text" name="colors" value="<%= product ? product.colors : '' %>"><br>
            <label for="price">Precio:</label>
            <input type="number" name="price" step="0.01" value="<%= product ? product.price : '' %>"><br>
            <button type="submit">Guardar</button>
        </form>
    <%- include('./partials/footer')%>
    </body>
</html>
